# alembic.ini — Alembic migration configuration for AgentOS Control Plane
# Run: alembic upgrade head
# See: https://alembic.sqlalchemy.org/en/latest/tutorial.html

[alembic]
# Path to migration scripts
script_location = %(here)s/alembic
prepend_sys_path = %(here)s


# Template used to generate migration file names
file_template = %%(year)d%%(month)02d%%(day)02d_%%(hour)02d%%(minute)02d_%%(slug)s

# Database URL — can be overridden via DATABASE_URL env var (see env.py)
sqlalchemy.url = postgresql://agentos:agentos_secret@localhost:5432/agentos_db

# -- logging --
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
